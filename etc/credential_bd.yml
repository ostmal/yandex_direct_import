# Параметры доступа к БД
database: postgres
user: postgres
password: ***********
host: localhost

# Наименование таблицы
schema: yd
table_name: yd

# Префикс к таблицам
table_prefix: ost_

# В скрипте yd.py наименование таблицы: [table_prefix][table_name]
# В скрипте yd_many_reports.py наименование таблицы: [table_prefix][Наименование Отчета (как в ЯД)]

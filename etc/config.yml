# --- Параметры запуска ---

# Записывать с указанием конкретной даты начала и конца периода (yes/no)
date_range: yes

# Записывать в файл scv (yes/no)
write_to_csv: no

# Путь к файлу csv
path_csv: csv/